<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css"
    integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous" />
  <meta http-equiv="X-UA-Compatible" content="ie=edge" />
  <title>Daily article</title>
</head>

<body>
  <%- include('_navigation') %>

    <div class="container">
      <h1 class="mb-1">
        <%= formatedDate %>
      </h1>

      <a href="/admin/daily" class="btn btn-secondary">All Articles</a>
      <% if(error){ %>
        <h2 class="text-danger">
          <%= error %>
        </h2>
        <% } %>

          <div>
            <form action="/admin/daily/<%= article.id %>?_method=PUT" method="POST">
              <div class="form-group">
                <label for="date">Date</label>
                <input required value="<%= formatedDate %>" type="date" name="date" id="date" class="form-control" />
              </div>
              <div class="form-group">
                <label for="description">Description</label>
                <textarea name="description" id="description" class="form-control">
<%= article.description %></textarea>
              </div>
              <div class="form-group">
                <label for="content">Content</label>
                <textarea required name="content" id="content" class="form-control">
<%= article.content %></textarea>
              </div>

              <a href="/admin/daily" class="btn btn-secondary">Cancel</a>
              <button type="submit" class="btn btn-primary">Save</button>
            </form>
          </div>
    </div>
</body>

</html>